<% content_for :title, "AutoGallery | Editar vehículo" %>

<div class="row justify-content-center">
  <div class="col-lg-8">
    <div class="detail-card p-4 p-md-5 fade-in-up">
      <p class="section-label mb-2">Editar vehículo</p>
      <h1 class="h2 fw-bold mb-2"><%= @vehiculo.modelo %></h1>
      <p class="support-copy mb-4">Actualiza los datos del vehículo y marca.</p>

      <%= render "form", vehiculo: @vehiculo %>
    </div>
  </div>
</div>
